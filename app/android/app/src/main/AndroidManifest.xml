<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Ежедневные напоминания (flutter_local_notifications). Используем
         НЕточные будильники, поэтому SCHEDULE_EXACT_ALARM не нужен. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Установка скачанного APK при обновлении из приложения (sideload). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Онлайн-перевод, свои серверы, субтитры и плеер YouTube. Явно в main,
         чтобы не зависеть от транзитивного merge из плагинов в release-сборке. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- «Повтори за диктором»: запись собственного голоса для сравнения с
         эталоном (services/voice_recorder.dart). Ничего никуда не уходит —
         дорожка живёт в кэше до конца карточки. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Доступ к галерее (READ_MEDIA_IMAGES/VIDEO/AUDIO) приезжает транзитивно
         из open_filex и приложению не нужен ни в одном сценарии:
           • фото для карточки и снимок для распознавания текста берёт
             image_picker — на Android 13+ это системный «Выбор фото», а он
             работает без разрешений вовсе;
           • книги и резервные копии открывает file_picker через системный
             диалог (SAF) — тоже без разрешений;
           • open_filex у нас открывает только скачанный APK из собственной
             внешней app-папки (UpdateService.downloadApk), а для своего
             каталога плагин разрешения не спрашивает.
         Google Play запрещает эти разрешения там, где хватает системного
         выбора файлов (правило «Фото и видео»), поэтому вырезаем все три. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <application
        android:label="Fern"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Приём текста/ссылок из других приложений через «Поделиться»
                 (receive_sharing_intent → ShareImport). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/*"/>
            </intent-filter>
            <!-- Fern в системном меню выделения текста: выделил слово в чужом
                 приложении — пункт «Fern» рядом с «Копировать» (MainActivity →
                 канал fern/process_text → services/process_text.dart). -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Приёмники для отложенных уведомлений (перепланирование после
             перезагрузки/обновления приложения). -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Нужно flutter_tts на Android 11+, чтобы находить движок озвучки. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
</manifest>
